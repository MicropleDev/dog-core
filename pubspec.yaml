name: dog_core
description: A minimal MVVM helper library with LiveResult, StateObserver, and API response handling.
version: 0.0.1
environment:
  sdk: '>=3.4.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
